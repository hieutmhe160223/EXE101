spring:
  application:
    name: backend
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/exe101_backend?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:sa123}
    driver-class-name: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    open-in-view: false
    show-sql: false
  h2:
    console:
      enabled: false
  sql:
    init:
      mode: never
server:
  port: ${SERVER_PORT:8081}

momo:
  partner-code: MOMOBKUN20180810
  access-key: 72U5IIA6ST3STUO4
  secret-key: 6zfox05Yv2tB6re8687fEw02n9v1Yv0J
  endpoint: https://test-payment.momo.vn/v2/gateway/api/create
  redirect-url: http://localhost:5173/order/success
  ipn-url: ${MOMO_IPN_URL:http://localhost:8081/api/payments/momo/webhook}

vietqr:
  bank-code: ${VIETQR_BANK_CODE:MB}
  account-number: ${VIETQR_ACCOUNT_NUMBER:0969823151}
  account-name: ${VIETQR_ACCOUNT_NAME:NGUYEN TA VAN}

wallet:
  topup:
    enabled: ${WALLET_TOPUP_ENABLED:true}
    min-amount: ${WALLET_TOPUP_MIN_AMOUNT:10000}
    max-amount: ${WALLET_TOPUP_MAX_AMOUNT:50000000}

sepay:
  webhook-api-key: ${SEPAY_WEBHOOK_API_KEY:yufiz_sepay_secret_key_2026_xyz}
  gateway: ${SEPAY_GATEWAY:MBBank}

apify:
  token: ${APIFY_TOKEN:}
  base-url: https://api.apify.com/v2
  actor-item-detail: zen-studio~goofish-xianyu-item-detail-scraper

rapidapi:
  key: ${RAPIDAPI_KEY:}
  translate:
    host: google-api31.p.rapidapi.com
    url: https://google-api31.p.rapidapi.com/gtranslate

exchange-rate:
  api-url: https://open.er-api.com/v6/latest/CNY
  cache-ttl-minutes: 60
