spring:
  datasource:
    url: jdbc:h2:mem:exe101-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  sql:
    init:
      mode: never
  h2:
    console:
      enabled: false

vietqr:
  bank-code: VCB
  account-number: '0000000000'
  account-name: EXE101

momo:
  partner-code: MOMOBKUN20180529
  access-key: F8BBA84256C589CF
  secret-key: local-secret-key
  endpoint: https://test-payment.momo.vn/v2/gateway/api/create
  redirect-url: http://localhost:8080/api/payments/momo/return
  ipn-url: http://localhost:8080/api/payments/momo/ipn
