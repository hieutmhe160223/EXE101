spring:
  datasource:
    url: jdbc:h2:mem:exe101-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  sql:
    init:
      mode: never
app:
  seed-demo: true
momo:
  partner-code: ""
  access-key: ""
  secret-key: ""
  ipn-url: ""
vietqr:
  bank-code: TEST
  account-number: "0000000000"
  account-name: TEST ONLY
logging:
  level:
    root: WARN
